# Config templates — Concept

Status: **Reserved** — no contract yet.

`config_templates/` is reserved in this repository's layout, but no format or contract for config
template content has been defined. Nothing may be authored here until one exists.

Next step: run `/concept` to work out the format and contract for this content type before any
story implements it.
