---
template: cover
title: A cover slide with no image
order: 20
---

This entry declares the `cover` template but carries no `image` field, so
the pipeline falls back to delivering it as a `text` slide instead of
dropping it, since title and body are both present.
